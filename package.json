{
  "name": "arcadeforge-game-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A self-hosted arcade with 1,200+ original playable game variants and a restricted allowlist web proxy.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
